```yml
committee:
  selection:
    criterion:
      description: Criterion for committee member selection (Art. 26 SR 0.103.3)
      metadata:
        unit: str
      values:
        2024-01-01:
          value: 'existing'
          description: Select existing members based on previous criteria.

      # Alternative criteria available
      # - value: 'new'
      #   description: Elect new members based on specified selection method.
      # - value: 'random'
      #   description: Random selection process.
```
