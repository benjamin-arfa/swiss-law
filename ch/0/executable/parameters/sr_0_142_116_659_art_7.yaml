(Note that since this legal article describes a required documentation format for requests, rather than a parameterizable calculation, we create a variable for storing this string, but do not have a parameter to update.)
